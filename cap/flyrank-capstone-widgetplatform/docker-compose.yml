version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      JWT_SECRET: changeme_use_a_long_random_string
      PORT: 3000
      WIDGET_VERSION: 1.0.0
      BASE_URL: http://localhost:3000
